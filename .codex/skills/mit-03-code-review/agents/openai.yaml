interface:
  display_name: "MIT-03 code review"
  short_description: "Validate TQA findings for all TB3 rubrics"
  default_prompt: "Use $mit-03-code-review to validate every TQA finding against the TB3 task evidence."

policy:
  allow_implicit_invocation: false
